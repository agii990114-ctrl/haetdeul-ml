# 2026-08-25 (화) — 취몽 데일리 로그

> 각 블록의 내용을 취몽 웹 데일리 로그 폼의 해당 필드에 붙여넣으세요. 글자 수 제한이 검증되어 있습니다.

| 필드 (`Field`) | 값 (`Value`) |
|---|---|
| **카테고리 (`category`)** | `final` |
| **상태 (`status`)** | `completed` |

## title (26/40)
```text
배치 파이프라인 무인화 및 단기 채점 착시 규명
```

## feature_name (43/100)
```text
일별 무인 배치 파이프라인 구축, 운영 모델 확정 및 타 파트 연동 스펙 설계
```

## problems (84/200)
```text
수동 실행에 의존하던 파이프라인을 자동화해야 했고, 45일 단기 실전 채점 시 모델이 전일가보다 뒤처지는 것으로 나타나 모델의 유효성에 의문이 제기됨.
```

## solution (101/200)
```text
run_batch.py로 E2E 파이프라인을 통합해 윈도우 스케줄러에 등록하고, 채점 구간을 8개월로 확장 검증함. 달력일 D+N 변환 책임을 ML이 담당하는 연동 테이블을 설계함.
```

## result (89/200)
```text
79초 만에 19.8만 행 재생성 및 추론을 무인 완료하는 체계를 확립했고, 8개월 확장 채점에서 소매가 8개월 중 7개월 승리를 증명하며 실전 가치를 입증함.
```

## content (792/6000)
````markdown
## 배치 파이프라인 무인화
`수집 → 학습테이블 재생성 → 추론 → 예측 적재 → 채점`을 단일 명령(`python run_batch.py`)으로 완결하고 단계별 실패 시 즉시 중단(Fail-fast)하는 파이프라인을 완성했다. 윈도우 작업 스케줄러(09:00 실행)에 등록하여 무인 운영을 개시했다.

## 운영 모델(`ops_*`) 확정
- 학습 기간: 2017~2023년 (검증셋 분리 없이 전 기간 학습, fixed 트리 수 적용)
- 검증 결과: 점 예측 및 분위수 구간 모두에서 기존 분할 모델 대비 동등 이상의 성능을 달성하여 운영 표준으로 채택했다.

## 단기 채점의 착시 규명 (45일 vs 8개월)
2026년 6~7월(45일치) 데이터로 첫 실전 채점을 돌렸을 때 "모델이 앵커보다 못하다"는 결론이 나왔다.
- **원인**: 해당 45일은 소매가가 계절적으로 극도로 정체되어 앵커가 유리한 특수 구간이었다.
- **조치**: 당일 오후 1~8월(8개월) 전체로 채점 구간을 확대했고, 소매가는 8개월 중 7개월(7월 제외)에서 앵커를 크게 이겼음을 확인했다. 단기 표본에 휩쓸리지 않고 충분한 시계열 구간으로 검증해야 한다는 교훈을 얻었다.

## 타 파트 연동 스펙 설계
구매 에이전트가 요구하는 달력일(`D+1~D+18`)과 ML 모델의 `영업일(Lead)` 간 변환을 ML 파트가 직접 책임지도록 `haetdeul.ml_price_forecasts` 테이블을 설계하고, `CHECK (target_dt = base_dt + offset_days)` 제약조건으로 데이터 무결성을 보장했다.
````
