# 2026-09-04 (금) — 취몽 데일리 로그

> 각 블록의 내용을 취몽 웹 데일리 로그 폼의 해당 필드에 붙여넣으세요. 글자 수 제한이 검증되어 있습니다.

| 필드 (`Field`) | 값 (`Value`) |
|---|---|
| **카테고리 (`category`)** | `final` |
| **상태 (`status`)** | `completed` |

## title (31/40)
```text
침묵 모니터링 결함 3건 척결 및 품목별 개별 분할 롤백
```

## feature_name (45/100)
```text
기준일 지연 픽스, 검증 쿼리 파이프라인 분리, 품목별 개별 모델 분할 실전 검증
```

## problems (112/200)
```text
기준일이 8개월간 1일씩 지연 생성되던 결함, SQL 검증 결과를 파이썬 커서가 버리던 구조, 3폴드를 통과한 양파 개별 분할 모델이 2026년 실전에서 배추 성능을 -5.7% 깎아먹는 역전이 발생함.
```

## solution (111/200)
```text
추론 입력 생성 시 관측 직후 익일부터 당일까지 빈 기준일을 자동 보정하고, 검증 전용 쿼리 파일을 신설하여 BAD 발생 시 배치를 즉각 정지하도록 수정함. 품목 분할을 기각하고 통합 모델을 유지함.
```

## result (93/200)
```text
248일 중 26일에 불과했던 D+1 계약 일치율을 100%로 정상화했고, 7개 컬럼의 NULL 방치를 차단했으며, 7개년 장기 학습 시 통합 모델의 우위성을 입증함.
```

## content (893/6000)
````markdown
## 1. "있는데 아무도 안 본다" — 침묵 결함 3건 척결
1. **기준일(Base Date) 1일 지연 결함 (8개월간 방치)**:
   - D일 기준일 행은 D-1일까지의 데이터만 써야 하는데, D일의 조사값이 DB에 들어온 후에야 D일 행을 생성하는 로직으로 인해 매일 아침 예측이 하루 전 기준일(D-1)로 나가고 있었다.
   - 추론 입력 단계에서 마지막 관측일 익일부터 당일까지의 빈 기준일 행을 생성하도록 수정하여 구매팀 계약(`daily[0] = as_of + 1`) 일치율을 정상화했다.
2. **배치 검증 쿼리 증발 결함**:
   - 파이썬에서 `cur.execute(sql)` 후 `while cur.nextset(): pass`로 돌리면서 SQL 파일 안의 수많은 검증 쿼리 결과가 콘솔에 찍히지 않고 증발하고 있었다. 이로 인해 파생변수 7개가 일주일간 NULL이었던 사실을 아무도 몰랐다.
   - 배치가 직접 읽고 판정하는 독립 검증 파일(`verify_rebuild.py`)로 분리하여 `BAD` 판정 시 배치를 즉각 중단하도록 개선했다.

## 2. 품목별 개별 모델 분할: 3폴드 통과 후 실전 역전으로 기각
양파 경락가를 별도 전용 모델로 분리하는 안이 3개 폴드(A, B, C)에서 모두 크게 개선(+7.19% / +5.48% / +19.20%)되어 채택 직전까지 갔다.
- **2026년 실전 164일 전수 평가**: 양파는 +2.67% 올랐으나 배추가 -5.70%로 폭락하여 통합 개선율이 +0.08%에 그쳤다.
- **원인**: 3~5년 폴드와 달리 프로덕션은 7년치 데이터를 쓰며, 데이터가 길어질수록 3개 품목을 함께 묶어 학습하는 통합(Pooled) 모델의 일반화 능력이 훨씬 뛰어났다. 이에 따라 품목 분할을 기각하고 통합 모델을 유지했다.
````
