# 일일 배치 후 점검 — 2026-09-09 (수)

점검 시간 09:23:18 – 09:26:56 (KST) · 무인 점검 · **변경 사항 없음**

---

## 결론

**배치 작업은 정상이고, 인수인계 테이블에는 오늘의 기준일이 들어갔습니다. 두 가지 보고 사항이 있습니다** —
① 데이터 품질 검사에서 문제가 나왔지만, 이는 **이틀치 데이터를 바탕으로 만든 가짜 경고**입니다 (실제 고장이 아님).
② 구매팀에 보내는 테이블의 **형태가 오늘 바뀌었습니다** — 가장 가까운 날짜(리드 1~2일)의 12개 행에 **어제 가격 대신 예측 모델 값**이 들어갔습니다. 어젯밤에 담당자가 의도적으로 바꾼 것이며, 고장이 아닙니다.

---

## 1. 일괄 처리 결과: 정상 — 실패 0건

`python agent/batch_agent.py` → 판정 **정상 (OK)**

```
run 65   started 2026-09-09 09:00:02 · 11 succeeded · 0 failed
         no day was skipped · last hand-off 0 hours ago
```

**연속으로 실패한 단계는 없습니다. 에러 문구도 없습니다.**

### 참고 사항 · 확인 도구가 cp949 콘솔에서 또 멈췄습니다 (사흘째 · 미해결)

```
UnicodeEncodeError: 'cp949' codec can't encode character '—' in position 322
   agent/batch_agent.py:520 -> 480   print(rep.text())
```

원인은 글자 하나입니다. 출력에 쓰인 긴 대시(`—`) 때문입니다.
`PYTHONIOENCODING=utf-8`을 설정하면 정상 실행됩니다 (위의 결과도 그렇게 뽑았습니다). 09-07 및 09-08 보고서에도 이미 적어둔 내용입니다.
**일괄 처리 자체와는 아무런 관련이 없습니다** (일괄 처리는 별도의 로그를 남깁니다).
하지만 **성공한 확인 작업이 실패한 것처럼 보인다는** 문제가 있습니다.
지시하신 대로 수정하지 않고 그대로 두었습니다.

---

## 2. 데이터 품질: **문제 1건 — 조사 완료, 오탐지**

`python agent/quality_agent.py --days 180` → 판정 **문제 (Problem)**
(직전 사흘은 모두 정상이었음.)

### 경고가 울린 이유

```
*** 등급 순서 (상급 >= 차상급)  최악의 역전율 50%
      배추 역전됨           1/2일 (50%)     <- 어제는 0/1일
      무 역전됨             4/103일 (4%)
      양파 역전           1/23일 (4%)
```

규칙: 역전율이 30%를 넘으면 문제입니다.
**배추는 전체 표본 기간이 2일뿐입니다.**

### 행 데이터를 직접 확인한 결과 — 50kg 거래 한 건이 이 수치를 만들었습니다

우리의 목표 포장 규격(그물망/파레트 10kg) 기준으로, 지난 **180일 동안 차상급 배추가 단 이틀**만 거래되었습니다. 그 이틀의 데이터는 다음과 같습니다.

```
2026-09-02   상급 836.2 KRW (286,360 kg · 3건)   차상급 750.0 KRW (   90 kg · 1건)   정상
2026-09-08   상급 647.6 KRW (518,140 kg · 4건)   차상급 750.0 KRW (   50 kg · 1건)   역전
```

**어제 들어온 단 한 건의 50kg 거래**가(전체 518톤의 0.0097%) 상급 평균보다 높게 들어왔고, 이 때문에 2일 중 1일(50%) 역전된 것으로 계산된 것입니다.

차상급 배추는 보통 **8kg 상자**로 거래됩니다(180일 동안 207건 · 710톤). 우리 규격은 이를 다른 상품으로 제외합니다(CLAUDE.md 4절). 그 결과 남는 데이터는 **180일 전체 통틀어 10kg 그물망 규격의 차상급 2건, 140kg**이 전부입니다.

**이것은 08-27 포장 혼합 사고와는 성격이 다릅니다.** 그때는 상급 평균 자체가 오염되었습니다(실제 711 KRW 대신 939 KRW).
오늘의 상급 숫자는 정상입니다 —

```
배추, 상급, 당사 규격, 최근 8영업일   611 – 923 KRW   09-08은 647.6
```

### 나머지 세 가지 점검은 정상입니다

```
일일 내 가격 격차    최악의 평균 1.7배 (배추)      2.5배 미만으로 정상
시계열 건전성      최저 0.642 (배추 · 150일)     0.60 이상으로 정상
기준가격 일치      불일치 0행 / 2,299행 x 3개 품목 0%
```

배추 자기상관계수 0.642는 어제(0.642) 및 그저께(0.651)와 사실상 같습니다. 변동이 없습니다.

### 참고 사항 (수정 안 함)

**이 검사 로직에는 최소 표본 수와 최소 물량 기준이 없습니다.** 차상급 배추 10kg 그물망처럼 **거래가 거의 없는 칸에서는, 거래가 단 한 건만 들어와도 역전율이 0%, 50%, 100% 사이를 널뛰기합니다.** 매일 울리는 경고는 결국 아무도 보지 않게 된다는 것을 이 프로젝트는 이미 학습했습니다(CLAUDE.md 9절). 판단은 사람이 직접 하도록 둡니다.

---

## 3. 오차(Drift): 경고 아님 — **3주 연속으로 진 조합은 없습니다**

`python agent/drift_agent.py` → 판정 **주의** (경고 기준은 3주 연속 패배입니다)

**숫자는 어제와 같습니다.** 09-07 주는 아직 점수가 나오지 않아서 분석 기간이 움직이지 않았습니다.

```
auc 배추            최근 3주 중 2주 패배   08-24 오차 +3.6%p · 08-31 오차 +1.6%p   (지난 8주 중간값 +4.9%)
auc 양파            최근 3주 중 1주 패배   08-31 오차 +1.9%p                      (지난 8주 중간값 +10.9%)
whsl 배추           최근 3주 중 1주 패배   08-24 오차 +10.6%p                     (지난 8주 중간값 +58.8%)
나머지 6개 조합은 기준 모델과 같거나 더 좋습니다
```

(오차 = 모델 오차 빼기 기준 모델 오차. 양수는 모델이 진 주를 뜻합니다.)

**모델을 다시 학습시키지 않았습니다.** 자동 재학습 판정 에이전트도 똑같은 결론을 냈습니다 (판정 주의 · 후보 없음).

> **내일 확인할 점**: 경매 배추의 09-07 주 점수가 나오면, 그 주를 먼저 확인하세요. 또 졌다면 3주 연속입니다. (어제 보고서에 쓴 내용과 같습니다.)

---

## 4. 구매 전달표: 기준일은 오늘인데, **예측 방식이 바뀌었습니다**

예측 표에서 그대로 읽은 내용입니다 (조회 전용) —

```
base_dt 2026-09-09 (오늘)   162 rows = AUC 54 + RTL 54 + WHSL 54
target dates                 2026-09-10 – 2026-09-27
loaded at                    2026-09-09 09:08:10
rows with empty predicted    0
```

**계약 `daily[0] = as_of + 1`은 유지됩니다** (기준일 09-09, 첫 예측일 09-10).

### 어제와 달라진 점 — 가까운 날짜들에 기준가격 대신 모델 값이 들어갑니다

기준가격(어제 가격) 그대로 나간 행의 수 (모델 값 대신 사용된 것):

```
              09-07   09-08   09-09 (오늘)
AUC  gated       6       6       0        <- 사라짐
RTL  gated       6       6       0        <- 사라짐
WHSL gated      22      22      26        <- 그대로임 (리드 1-2일에 12개 행 추가)
```

무(AUC)를 예로 듭니다:

```
09-08   예측 1일 및 2일 전   기준가격 605원 그대로 통과 (게이트 켜짐)
09-09   예측 1일~4일 전     모델 값 524원 적용          (게이트 꺼짐, 분위수 밴드)
```

### 원인 — 오류가 아니라, 어젯밤에 의도적으로 바꾼 것입니다

`run_batch.py` 파일에 **2026-09-08 자로 새로운 게이트 설정이 들어 있습니다.**

```python
GATE_LT = {"auc": 0, "whsl": 3, "rtl": 0}      # 0은 게이트 없음 의미
```

주석에 재결정 근거가 기록되어 있습니다 (2026년 운영 기간 측정 기준).

```
auction     979 rows   error 10.06% -> 9.98% (+0.7%)   direction 58.5% -> 63.8% (+5.3%p)
retail      993 rows   error  3.99% -> 4.07% (-2.1%)   direction  5.8% -> 56.0% (+50.2%p)
wholesale   993 rows   error  3.93% -> 5.60% (-42.5%)  -> 도매만 게이트를 유지하는 이유입니다
```

실험 결과물들의 시간 기록이 같습니다 (2026-09-08 18:52-18:55).
**오늘 배치 작업은 이 설정으로 실행된 첫 번째입니다.**

### 사실 관계만 (평가는 제외, 바뀐 것은 없음)

```
1. 변경 사항이 아직 커밋되지 않았습니다 - git status에 " M run_batch.py"로 나옴
   (run_batch.py 마지막 커밋은 2026-09-07 16:46)
2. CLAUDE.md에는 여전히 --gate-lt 3이 권장 운영값이며(5.9),
   리드 1-2일이 기본값보다 나쁘다고 되어 있습니다(섹션 8). 문서와 
   실제 운영 내용이 서로 다릅니다
3. 구매팀이 받는 표에서, 리드 1-2일 행들의 성격이 바뀌었습니다
   (is_gated=false, AUC의 경우 band_method='quantile'). 구매팀에 
   사전 고지되었는지는 이 점검으로 확인 불가합니다
```

---

## 참고 · 배치 작업에서 남긴 수집 확인 기록 (참고용)

수집 과정에서 **주의** 경고가 2건 발생했습니다. 둘 다 확인해 보았으며, 오늘의 예측에는 전혀 영향이 없습니다.

```
최근 일자의 경매 행 수 248개 (기준 458개 · 54%)
   -> 최근 8영업일 동안 행 수는 197~273개였습니다. 09-03에만 497개여서 기준치가 올라간 것으로 보입니다. 248개는 최근 일자 기준으로 정상입니다.
   -> 우리가 목표로 하는 규격(상품)은 09-01부터 09-08까지 모든 영업일에 3가지 농산물 모두 있습니다 (09-08 기준: 무 642 t, 배추 518 t, 양파 373 t). 09-03처럼 목표 물량이 아예 비어 있는 구간은 없습니다.

지난 7일간 날씨 강수량 누락 83.2% (평소 54.1%)
   -> 누락된 데이터가 아닙니다. 비가 오지 않으면 칸이 비어 있는 것입니다.
```

---

## 오늘 한 일 / 안 한 일

```
did        에이전트 3개를 돌리고, 원본 경매 데이터를 직접 조회하고, 인수인계
           표를 읽고, run_batch.py 변경 사항을 검토하고, 어제 로그와 비교함
did not    파일 수정 0개, 배치 재실행 0회, 재학습 0회, 데이터베이스 쓰기 0회
```