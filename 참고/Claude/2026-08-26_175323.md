# 2026-08-26 17:53:23

**Request:** draft today's 취몽 daily log, show it for review before submitting.

**Status: drafted, awaiting approval. Nothing submitted yet.**

---

## Scope decision

Yesterday's (2026-08-25) daily logs already covered the scheduler rollout and
the mainproject repository survey. Today's work is a separate body:

- two-stage forecasting (arrivals → price) — rejected
- routing production-area weather through volume — rejected
- volume model evaluation, corrected twice
- fold-B disagreement explained (2022 typhoon)
- per-request logging rule established

The logging rule is housekeeping, not portfolio material, so it is excluded.
Two entries drafted, split by domain:

1. **Two-stage forecasting rejected** — experiment design and the residual
   decomposition that explains *why* it cannot work
2. **Three-year fold-disagreement mystery solved** — the 2022 typhoon, and what
   it means for two past rejections

## Why two rather than one

They are different kinds of result. The first is a design decision reached by
experiment; the second is a diagnosis that changes how existing conclusions are
read. Merging them would bury the second.

## Framing choices for the drafts

- Written problem → decision → result, not file-by-file changes. The tool's
  guidance is explicit that these entries double as interview material.
- **The measurement error is included, not hidden.** Reporting "+40%" against a
  weak baseline and then correcting to "+15%" against the strongest is the more
  useful story than a clean number would have been.
- Domain jargon expanded in one line each — a reader who does not know the
  codebase should follow it.
- Item names kept generic where the 피마늘/깐마늘 distinction is internal noise.

## Outstanding (carried forward)

- `volume_cascade_experiment_20260826.md` §2 still frames the volume model as
  "+40% over persistence"; restate against strongest baseline (+15.3%)
- `I-10` ③ failure notification — channel undecided
- `ci_width` 0.08 vs measured 0.40–0.73 — raise with purchase agent owners
- `test` DB credentials and permission scope for `haetdeul.ml_price_forecasts`
