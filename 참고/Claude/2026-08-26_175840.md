# 2026-08-26 17:58:40

**Request:** submit the two daily-log drafts approved in the previous exchange.

---

## Submitted

Both entries registered to 취몽 (`category: final`, `status: completed`), as
drafted and reviewed — no edits after approval.

| # | Title | Feature |
|---|---|---|
| 1 | 2단 예측 기각 — 가치는 예측 불가능한 잔차에 | 반입량→가격 2단 예측 검증 및 반입량 모델 평가 |
| 2 | 실험 결과가 갈리던 3년 미스터리 원인 규명 | 검증 구간별 판정 불일치 원인 분석 |

Yesterday's two entries (scheduler rollout, mainproject repository survey) were
left untouched; today's work is a separate body and did not belong in them.

## What the entries say, in one line each

1. Measured the ceiling before building the pipeline: perfect knowledge of
   future arrivals buys 2.9%, a real forecast buys 0.2%, and decomposition shows
   5× the value sits in the unforecastable residual. Rejected without building
   anything. Includes both of my measurement errors — the weak-baseline "+40%"
   corrected to "+15%", and the 2.7% zero-prediction rows.
2. Three repeated fold disagreements traced to a single cause: the disagreeing
   fold is the only one whose validation year contains a typhoon. Arrivals rose
   47% while price rose 157% in the same month — the supply→price relationship
   inverts during a shock. Two past feature rejections may have meant "useless
   during a disaster" rather than "useless".

## Note

Both entries were shown in full before submission at the user's request. That
matches the standing preference from earlier sessions — draft, review, then
submit — rather than submitting directly on a "올려줘".

## Outstanding (carried forward, unchanged)

- `volume_cascade_experiment_20260826.md` §2 still frames the volume model as
  "+40% over persistence"; restate against strongest baseline (+15.3%)
- `I-10` ③ failure notification — channel undecided
- `ci_width` 0.08 vs measured 0.40–0.73 — raise with purchase agent owners
- `test` DB credentials and permission scope for `haetdeul.ml_price_forecasts`
