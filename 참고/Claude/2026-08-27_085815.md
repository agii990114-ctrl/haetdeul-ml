# 2026-08-27 08:58:15

**Request:** restate the volume-model figures in the experiment record against the
strongest baseline, replacing the "+40% over persistence" framing.

---

## What was wrong

`진행기록/volume_cascade_experiment_20260826.md` §2 reported the volume model as
**+40% over persistence**. Arrivals are strongly seasonal, so yesterday's value is
the *weakest* of the three candidate baselines. Against the strongest
(previous-year same period) the figure is **+15.3%**.

The daily logs submitted yesterday already carried the corrected number, so the
project record was the only place still stale.

## Changes

**`진행기록/volume_cascade_experiment_20260826.md`**

- Header carries a dated correction notice pointing at §2
- §2 gains two subsections: *Second correction — persistence is the wrong yardstick*
  (with the three-baseline table) and *A third slip, caught while recomputing*
  (the 2.7% zero-prediction rows)
- §2 horizon profile restated as percentages against the strongest baseline;
  removed a duplicated absolute-WMAPE block left over from the first draft
- §3 year table rebuilt against previous-year rather than persistence, and extended
  from 5 sampled years to all 10, which exposes a fact the sample hid — **the model
  needs ~4 years of history**: 2017 −17.9%, 2018 −3.8%, consistently positive from
  2019
- §7 gains a fifth conclusion: always report against the strongest of several
  baselines

**`진행계획/프로젝트_백로그_v2.md`** — `M-16` restated: +15.3% with per-item
figures, the lead-time insensitivity (LT1 +13.0% → LT18 +14.8%), a concrete error
statement (109 t off on a 606 t day; one day in ten misses by ≥39%), and the
four-year training prerequisite as an explicit precondition.

## Deliberately left alone

- The two `+40%` mentions that survive in the experiment record are the correction
  narrative itself and a "do not quote this" warning in the conclusions. Project
  convention is to leave mistakes visible rather than erase them.
- Earlier `참고/Claude/` request logs still contain the old figure. They are
  point-in-time records of what was believed then; rewriting them would defeat
  their purpose.
- `CLAUDE.md` §11 and §12 mention "+40%" as the worked example of the baseline
  trap. That is the intended use.

## Not measured again

No models were retrained. Every number used here came from runs already completed
on 2026-08-26; this was a documentation correction, not a new experiment.

## Outstanding

- `I-10` ③ failure notification — channel undecided
- `ci_width` 0.08 vs measured 0.40–0.73 — raise with purchase agent owners before
  wiring predictions through
- `test` DB credentials and permission scope for `haetdeul.ml_price_forecasts`
- `M-16` — ask logistics/purchasing whether a supply forecast is wanted at all
